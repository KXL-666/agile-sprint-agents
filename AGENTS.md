# 开发 Agent 工作契约

本文件适用于参与本仓库的任何编码 Agent。项目目标与范围以 `PROJECT_BRIEF.md` 为唯一依据。

## 执行顺序

1. 阅读 `PROJECT_BRIEF.md`。
2. 说明本次只做哪个功能、会改哪些文件、如何验证。
3. 先实现最小可验证版本，再补充界面与异常处理。
4. 运行测试或本地启动验证。
5. 交付时报告：完成内容、修改文件、验证命令、已知限制。

## 必须保留的产品边界

- 系统的核心是敏捷开发迭代协同，不是通用聊天机器人。
- 协同过程必须可见：角色、输入、输出、状态变化、退回原因、审批结果。
- 模型未实际调用时，必须明确标记为演示模式。
- 文件覆写、删除、命令执行、依赖安装和批量修改均需人工确认设计。
- Python 是唯一要求实现完整“修改—测试—修复”闭环的语言。

## 完成标准

一个功能只有同时满足以下条件才能标记完成：

- 有可操作的网页入口或 API；
- 数据被持久化；
- 失败路径有明确用户提示；
- 有测试或可复现的验证步骤；
- 不破坏现有敏捷协同状态机与审批边界。
