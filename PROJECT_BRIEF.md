# 项目总说明：AgileSprint Agents

## 1. 项目身份

- **毕业设计题目**：基于多智能体协同的敏捷开发任务管理系统的设计与实现
- **目标用户**：3～8 人的小型软件开发团队、学生开发团队
- **运行环境**：Windows 本地运行
- **技术基础**：Python、Flask、Bootstrap、SQLite（后续可迁移 MySQL）、自定义多智能体协同编排模块、兼容 OpenAI 接口的大模型 API
- **开发原则**：新项目独立实现；可使用开源框架，但不得把完整开源产品直接伪装为自研系统。

## 2. 一句话目标

用户输入一项软件需求后，项目经理、开发、测试、回顾四类智能体通过可追踪的消息、任务和反馈，完成一次敏捷迭代闭环。

## 3. 必须实现的业务闭环

```text
需求输入
→ 项目经理 Agent 分析、拆解和分配
→ 开发成员 Agent 生成方案或修改代码
→ 测试 Agent 执行/分析测试并给出通过或退回结论
→ 项目经理 Agent 仲裁、重分配或要求修复
→ 持续修复，管理员可随时补充要求、停止或人工介入
→ 回顾 Agent 生成迭代复盘报告
```

## 4. “真实多智能体协同”的最低标准

1. 每类 Agent 必须有独立职责、系统提示词、可读取信息和可使用工具。
2. Agent 之间的消息必须写入数据库，并在网页时间线中展示。
3. 上一 Agent 的结构化输出必须成为下一 Agent 的输入；禁止仅并列调用多个 API。
4. 测试 Agent 有权退回任务；项目经理 Agent 有权依据技能、负载和测试结果重新分配任务。
5. 人工管理员可选择“一键协同”或“逐阶段确认”。
6. 未配置模型时只能显示为“演示模式/规则引擎”，不得冒充真实模型调用。

## 5. 功能范围

### 必做

- 管理员登录与模型 API 配置；API Key 加密保存。
- 项目、迭代、需求、任务、团队成员与技能/负载管理。
- 四角色 Agent 协作、消息日志、状态机和项目经理仲裁。
- 需求拆解、任务分配、进度追踪、测试退回、持续修复、回顾报告。
- 本地文件或文件夹接入；支持新建独立文件/文件夹。
- 涉及覆盖、删除、执行程序、安装依赖、批量修改时，必须提供人工确认；“自动完成”模式在开始时进行一次明确授权，逐步确认模式则逐文件或逐操作确认；跳过后流程继续。
- Python 项目支持运行测试和持续自动修复建议；其他语言仅支持生成/编辑建议。
- 协作时间线和任务看板：让非技术评委能看懂“谁在做什么、为什么退回、下一步是什么”。

### 扩展

- 生成项目相关 Markdown、Word、Excel 文档，例如需求说明、测试报告、缺陷清单、迭代复盘。
- 多模型供应商：DeepSeek、通义、智谱、OpenAI、豆包；第一优先级为 DeepSeek。

### 明确不做

- 不做飞书、钉钉式通用办公平台。
- 不做无限制操作整台电脑的自动化；高风险操作永远需要确认。
- 不对 Java、前端、C++ 等语言承诺完整自动运行和修复闭环。
- 不在未验证成功时声称已经完成代码生成、测试或文件修改。

## 6. 核心数据与状态

- 实体：Project、Sprint、Requirement、Task、TeamMember、AgentProfile、AgentMessage、Approval、FileOperation、TestRun、RetrospectiveReport、ProviderConfig。
- 迭代状态：`draft → planning → developing → testing → rework → reviewing → done`；发生异常或管理员主动停止时进入 `blocked`。
- 任务状态：`todo → assigned → in_progress → testing → rework → done / blocked`。

## 7. 验收演示

答辩时至少演示一项 Python 功能需求，例如“为学生社团报名系统增加登录、报名与导出功能”：

1. 输入需求并选择一个本地 Python 项目或新建项目目录。
2. 页面清楚显示项目经理的任务拆解、三名开发 Agent 的分配理由。
3. 展示开发 Agent 的文件修改提案与逐文件确认。
4. 展示测试 Agent 的测试输出；至少准备一次退回与修复。
5. 展示项目经理重分配/仲裁，以及回顾 Agent 生成的迭代报告。

## 8. 对开发 Agent 的工作约束

1. 开始编码前，先阅读本文件和 `AGENTS.md`。
2. 每次只完成一个明确阶段，不得自行扩大为通用 Agent 平台。
3. 修改代码后必须运行相应测试或启动检查，并报告结果。
4. 所有 UI 文案优先使用中文，面向不懂技术的评委；不可只展示内部 JSON 或日志。
5. 新增开源依赖时必须记录用途、许可证和安装方式。
6. 不得输出或提交真实 API Key、密码、个人文件内容。
